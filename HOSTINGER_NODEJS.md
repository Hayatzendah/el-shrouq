# دليل ربط المشروع مع Hostinger كـ Node.js

## ✅ الملفات المطلوبة للتعرف على المشروع كـ Node.js:

1. **package.json** ✓
   - يحتوي على `"main": "index.js"`
   - يحتوي على `"engines": { "node": ">=18.0.0" }`
   - يحتوي على `"scripts": { "start": "node index.js" }`
   - يحتوي على `"keywords": ["nodejs", "nextjs", "node"]`

2. **index.js** ✓ (ملف الدخول الرئيسي)
3. **app.js** ✓ (ملف بديل)
4. **server.js** ✓ (ملف الخادم)
5. **.nvmrc** ✓ (إصدار Node.js: 18.20.0)
6. **.node-version** ✓ (إصدار Node.js: 18.20.0)
7. **runtime.txt** ✓ (nodejs-18.20.0)
8. **Procfile** ✓ (web: npm start)
9. **hostinger.json** ✓ (إعدادات Hostinger)
10. **app.json** ✓ (إعدادات المنصات)

## 🔧 خطوات الربط في Hostinger:

### الطريقة 1: الربط التلقائي من GitHub
1. اذهب إلى Hostinger → Node.js App Manager
2. اضغط "Connect GitHub"
3. ابحث عن مستودع `el-shrouq`
4. **إذا لم يظهر كمشروع Node.js:**
   - اضغط "Refresh" أو "Reload"
   - امسح الكاش في المتصفح (Ctrl+Shift+Delete)
   - جرب إعادة الاتصال

### الطريقة 2: الربط اليدوي
1. في Hostinger → Node.js App Manager
2. اضغط "Create New App"
3. اختر "From GitHub"
4. أدخل رابط المستودع: `https://github.com/Hayatzendah/el-shrouq`
5. **اختر نوع المشروع: Node.js** (حتى لو لم يظهر تلقائياً)
6. اضغط "Connect"

### الطريقة 3: رفع الملفات يدوياً
إذا فشلت الطريقتان السابقتان:
1. في Hostinger → File Manager
2. اذهب إلى مجلد Node.js Apps
3. أنشئ مجلد جديد: `el-shrouq`
4. ارفع جميع الملفات من المشروع
5. في Node.js App Manager:
   - اختر المجلد: `el-shrouq`
   - Build Command: `npm run build`
   - Start Command: `npm start`
   - Node.js Version: `18.x`

## ⚙️ الإعدادات المطلوبة في Hostinger:

### Build Command:
```
npm run build
```

### Start Command:
```
npm start
```
أو:
```
node index.js
```

### Node.js Version:
```
18.x
```
أو:
```
18.20.0
```

### Environment Variables:
أضف هذه المتغيرات في Hostinger:
- `NEXT_PUBLIC_FIREBASE_API_KEY`
- `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
- `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
- `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
- `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
- `NEXT_PUBLIC_FIREBASE_APP_ID`
- `NODE_ENV=production`
- `PORT=3000` (أو المنفذ المخصص من Hostinger)

## 🔍 التحقق من أن المشروع معرّف كـ Node.js:

افتح `package.json` وتحقق من:
- ✓ `"main": "index.js"` موجود
- ✓ `"engines": { "node": ">=18.0.0" }` موجود
- ✓ `"keywords"` يحتوي على `"nodejs"` أو `"node"`
- ✓ `"scripts": { "start": "node index.js" }` موجود

## 📝 ملاحظات مهمة:

1. **تأكد من رفع جميع الملفات** إلى GitHub
2. **تأكد من أن `package.json` صحيح** ولا يحتوي على أخطاء
3. **إذا لم يظهر المشروع:**
   - تحقق من أن المستودع عام (Public) أو أن Hostinger لديه صلاحية الوصول
   - جرب إعادة الاتصال بـ GitHub
   - اتصل بدعم Hostinger إذا استمرت المشكلة

## 🆘 استكشاف الأخطاء:

### المشروع لا يظهر في قائمة Node.js:
- ✓ تحقق من أن جميع الملفات المذكورة أعلاه موجودة
- ✓ تحقق من أن `package.json` صحيح
- ✓ جرب الربط اليدوي

### خطأ في البناء (Build):
- ✓ تحقق من متغيرات البيئة
- ✓ تأكد من إصدار Node.js (18.x)
- ✓ راجع سجلات الأخطاء في Hostinger

### الموقع لا يعمل بعد البناء:
- ✓ تحقق من Start Command
- ✓ تحقق من المنفذ (Port)
- ✓ تحقق من متغيرات البيئة

